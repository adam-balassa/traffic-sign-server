spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db?serverTimezone=UTC;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
detector:
  host: http://localhost:8082